<h1>Lessons</h1>
<% if !current_user.teacher %>
  <% @lessons.each do |l| %>
<ul> 
  <li><%= "#{l.instrument} #{l.time} #{l.date} #{l.location}"%></li>
</ul>
<% end %>
<% else %>
<%= link_to 'create a lesson', new_lesson_path %>

<% end %>





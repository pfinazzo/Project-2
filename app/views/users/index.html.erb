<h1>Users</h1>

<ul>
<% @user.each do |u| %>
<%= u.email %>
<% unless current_user.nil?%>
<% unless u.email == current_user.email%>
<li><%="  #{u.name} " %><%= "  #{u.email} " %>&nbsp;&nbsp;&nbsp;&nbsp;
<%= link_to "Delete", user_path(u), :method => :delete %>
<% end %>
<% end %>
</li>

<% end %>
</ul>
